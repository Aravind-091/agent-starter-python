LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Browser tools: false shows the browser window (headed) on this machine.
# Docker and CI set this to true; leaving it unset also defaults to headless.
BROWSER_HEADLESS=false
